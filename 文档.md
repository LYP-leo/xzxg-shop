# 关键问题解决方案
## 流式输出与渲染协议
（缺一句话导入）导购agent

## 多模态交互

## prompt动态拼接与成本管控
真实购物场景中用户的意图不同，导致理想的输出不同。

## 真实商品召回

## 记忆与改写
当前记忆模块做的较简单。在项目初期，我们考虑了两种产品形态：
1. 参考豆包，每次继承上次的对话
2. 参考gpt，每次打开时新开对话
我们分析电商场景下的特点：
1. 注意力分散（？），用户每一次新的购物可能是完全不同的需求，且和之前的需求没有太大关系。很可能之前某个需求满足了，如买到了心仪的洗面奶，接下来一段时间都不会考虑再购入洗面奶。与之对比，问答/对话式agent的场景下，用户的query可能是用户感兴趣的、或是生活与之有关的问题，可能会反复提及
2. 
每一轮对话开始前，会从库中检索最近N（目前为5）轮对话信息，由小模型判断注入react agent的prompt中。
（高亮块）bad case：

## 风控与安全
生产场景中，必然需要考虑到风控与安全的问题（导入语）
当前采用三个维度进行风控：
1. 用户query维度：用户某次输入问题中包含违规内容时（如涉及政治、黄赌毒等）命中风控。采用规则匹配，支持关键词/正则模式。
2. 用户维度：若某用户被标记为风险用户命中风控。
3. 商家/商品维度：若某商家/商品被标记为风险商家/商品，会在召回时过滤，不透出给后续大模型。

技术决策（高亮块/分栏对比）：用户query维度的风控，是使用小模型决策还是规则匹配
小模型决策优点：灵活
但是考虑到：
1. 一些新型针对大模型的攻击手段，如大模型投毒、精心设计提示词绕过大模型等等，目前应对手段可能也不是特别成熟，引入模型可能反而引入了风险
2. 预算控制，多一次模型调用导致多消耗token，在用户量大的情况下可能导致成本激增
3. 虽然很少，但是还是会稍微增加rt
最后采用了规则匹配的模式。

## 测评

我们建立了以下这些测评，每个关注xx指标。。。

## 开发范式

## 其他
1. 真实电商链路模拟：加购、下单
2. 用户登陆、鉴权等
