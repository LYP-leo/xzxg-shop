# Android APP 多模态输入流程图

下面流程以当前代码实现为准，概括 Android 导购聊天中 **文字、图片 / PDF 附件、语音** 三类输入如何统一进入 Agent 消息链路。

```mermaid
flowchart TD
    A["用户在 Android 聊天页输入"] --> B{"输入类型"}

    B --> T["文字"]
    B --> F["图片 / PDF 附件"]
    B --> V["语音"]

    T --> S["读取输入框文本"]

    F --> F1["选择相册图片或文件"]
    F1 --> F2["生成附件预览"]
    F2 --> F3["发送前上传到 /api/v1/files"]
    F3 --> F4{"上传是否成功"}
    F4 -->|"失败"| F5["保留输入和附件，提示重试"]
    F4 -->|"成功"| F6["得到 file_id / url / object_key"]

    V --> V1{"语音识别路径"}
    V1 -->|"优先"| V2["讯飞实时语音识别"]
    V2 --> V3["客户端录制 PCM，经 /speech/realtime 发给后端"]
    V3 --> V4["后端代理讯飞返回识别文本"]
    V1 -->|"兜底"| V5["Android 系统语音识别"]
    V4 --> V6["识别文本回填输入框"]
    V5 --> V6
    V6 --> S

    S --> M["组装用户消息"]
    F6 --> M
    M --> L["保存本地用户消息"]
    L --> C{"是否已有远程会话"}
    C -->|"没有"| C1["创建 Agent 远程会话"]
    C -->|"已有"| C2["复用远程会话"]

    C1 --> E["调用 messages:stream"]
    C2 --> E

    E --> R["后端创建 user_message 和 agent_run"]
    R --> A1["Agent Runtime：风控、附件归一化、Planner、工具调用"]

    A1 --> I{"是否需要按图找货"}
    I -->|"是"| I1["search_image_products"]
    I1 --> I2["用图片 file_id / object_key / url 生成向量"]
    I2 --> I3["Milvus 图片向量召回相似商品"]
    I -->|"否"| A2["普通导购工具链"]

    I3 --> O["SSE 返回结果"]
    A2 --> O

    O --> U["Android 实时渲染：思考、正文、商品卡、追问"]
```

简要说明：

- 文字输入直接进入普通消息发送链路。
- 图片和 PDF 会先上传到 `/api/v1/files`，聊天消息里只携带附件元信息。
- 语音先转成文本并回填输入框，用户确认后仍按普通文本消息发送。
- 图片找同款不是客户端直接完成，而是 Agent 在后端工具链中调用 `search_image_products`。
- 最终结果通过 SSE 流式返回，Android 端按事件渲染正文、商品卡和追问。
