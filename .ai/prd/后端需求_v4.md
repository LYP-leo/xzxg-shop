1. rag这块，分成几个类别，比如品牌词召回 品类词召回、等等类别，llm调用工具的时候决策这个，给到rag系统
2. 管理员页面的链路追踪，加一下每次llm_call的实际prompt
3. 关于agent
  - 我发现non_guide和fast_product其实有一些重复的地方，理一下他们的界限。guide和non_guide需要llm，fast_product感觉脚本那种就好了。或者把一些固定流程（比如跳转到购物车页面这些）包成skill等等，在non_guideagent需要的时候按需加载
  - 导购意图 子agent的prompt中，比如scene_solution，prompt是：
  ```
  当前导购意图是 P5/scene_solution：用户需要特定场景下的整体解决方案与清单。

可用工具与信息侧重：
- 搜索场景清单、核心品类、场景风险、地点/季节/人群相关信息。
- 先建立清单框架，再对核心品类给选购建议。
- 不要把场景方案写成单一商品推荐。

内容范式：
- 先介绍当前需求场景，说明目标、约束和容易遗漏的风险。
- 根据用户需求列出清单表格，建立品类认知框架。
- 对核心品类逐项说明怎么选，不只堆商品。
- 结合地点、时间、场景、用户画像和品类给最后建议。

输出要求：
- 文案约 900 字。
- 表格后输出一个 `<inventory>`。
- 在具体品类选择建议模块中穿插 `<buyer>` 与 `<item>`，二者必须一起出现。
- 末尾输出一个 `<further>`。

输出骨架：
```markdown
# [回应用户需求的简短标题]


【输出硬约束】
- 如果输出 <item>...</item> 挂品标签，<item> 内只能写已由工具返回的 product_id，例如 <item>p_001</item>；禁止写商品名、品牌名或自然语言挂品指令。
- 重点词、品牌词、系列词、属性词必须用 Markdown 加粗，例如 **耐克**、**防水**；禁止输出 special_word、special word、（special_word）等内部标识。
- search_knowledge 返回的资料片段只能作为依据引用，不要编造未出现在工具结果中的内容。
```
里面这个“可用工具与信息侧重”应该是动态组装可用工具（agent.prompt.tool_protocol），比如这个场景不需要update_cart_item这个工具，需要search_products，就把search_products的参数和使用规则这些拼接进prompt里


run_1779597014647992000 你可以看下这个日志，商品库里没有马桶数据，但是最后推荐了p_digital_001这些无关的商品