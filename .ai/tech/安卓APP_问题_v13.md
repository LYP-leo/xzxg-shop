1. 侧栏
  - 点击 “历史会话搜索” 之后，上面的四个按钮确实收起来了；但是当我把键盘收起来的时候，仍然聚焦在 “历史会话搜索” 的输入框中
    - 请你在用户收起键盘的时候，焦点就从输入框中移开
  - 在我输入内容之后，比如输入了个 “我”，下面的历史聊天中确实出现了几个包含关键字 “我” 的聊天，但是仅仅出现了一瞬间就消失了
    - 请你修复这个bug
  - 下面的用户和设置按钮，背景改回白色
    - 就像使用横线隔开四个按钮和历史会话一样，可以同样使用横线将历史会话与下面的用户和设置按钮隔开

2. 个人资料页面
  - 最左侧的 “取消” 按钮太靠上了
    - “取消” 按钮需要和右侧的 “保存” 按钮对齐