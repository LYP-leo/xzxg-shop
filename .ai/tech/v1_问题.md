## 问题

1. 用户从客户端发起请求时，是否要创建用户消息记录
  + 如果用户每次进入聊天窗口都是一次新的会话，这种情况需要创建新的会话记录
  + 如果每次进入加载了过去的聊天记录，那么就不需要创建新的会话记录
  + 我们可以采用第一种方式，用户每次进入聊天窗口都是一次新的会话。一方面省去了管理记忆的麻烦，另一方面

2. assistant placeholder 是什么？
3. 通过 Vision/OCR 得到图片解析结果 如果是一个没有文字的图片，比如鼠标图片，怎么识别？
  - 单次多模态大模型调用识别
  - rag检索相似商品
4. 14.4 Context Builder 构建上下文后面增加一个显式 query 改写环节
  例如：
  ```text
  上一轮：我主要给老人用，不要太贵。（最后推荐了a手机，b手机等）
  这一轮：那第一款怎么样？
  ```
  这一轮改为：那a手机怎么样？
5. "need_clarification": false,不需要这个，如果用户意图不明确，先输出一版回答，最后再追问
6. 意图需要分成几类（具体）
7. 为了方便前端解析，AgentResponse 该如何设计
8. RAG 构建部分，非结构化文档如何解析清洗切片？如何召回（意思相近的文档如何检索）
9. 对于用户不同的意图，这版架构会合并到同一个链路上，但是会根据用户的意图选择不同的工具。实际上，用户的意图不同，最后的 Decision Generator 需要不同的 prompt 驱动