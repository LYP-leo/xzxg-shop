1. 商品页面
  - 当我点击搜索栏的时候，为什么下面的bar（商品列表和活动）会被键盘推到上面来？应当被键盘覆盖在下面。
  - 当我点击 “加入购物车” 的按钮的时候，按钮应当出现被点击的效果，现在用户看不出来商品到底有没有被加到购物车里
    - 在商品页面右下角新增一个悬浮的 “购物车” 图标，右上角有一个红点，里面的数字代表购物车里面商品的数量，如果超过 99 就显示 99+
  - 当我从 “商品详情” 页面返回的时候，实际上是先返回页面最上方（一闪而过），然后再跳到我进入 “商品详情” 页面之前的位置的
    - 请你修复这一 bug，我从 “商品详情” 页面返回的时候，应当直接跳到原来的位置，而不是先返回页面最上方，再跳到我进入 “商品详情” 页面之前的位置

2. 购物车页面
  - 商品图片和数量框的加减号重叠了，请你给二者略微拉开距离
  - 购物车页面下方的明细，结算按钮上面显示的应该是 “商品总额：”，而不是“应付：”
    - “应付” 价格是商品总额减去优惠金额得到的结果
  - 购物车页面最下方，为什么有两个 “底框”？请你看图片 /Volumes/shared/xzxg-shop/.ai/tech/assets/order_bug.jpg 里面红框框出来的部分，请你把这部分多余的底框删掉

3. 设置页面
  - 在 “关于” 按钮下面新增一个 “高级设置” 的按钮，点击之后进入 “高级设置” 页面
  - 把 “测试后端” 也放到 “高级设置” 页面里

4. 侧栏页面
  - 当我点击搜索栏的时候，下面的显示 “用户头像、昵称” 和 “设置按钮” 的 bar 被键盘推上来了。同样，你需要让这个 bar 被键盘覆盖在下面
  - “返回聊天” 按钮放到高级设置页面当中，可选开启。
    - 若关闭，则侧栏中不显示 “返回聊天” 按钮