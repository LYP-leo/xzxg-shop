请你首先把所有页面右上角的用户设置的入口按钮删掉
  - 也就是左侧是头像，右侧是用户名，点击能够进入设置页面的那个按钮
  - 这个按钮会出现在几乎所有页面的右上角，但并没有什么用
  - 你需要去掉这个按钮

1. 登录页面
  - 登录页面必定是在未登录的情况下出现的，因此此时左上角的侧栏按钮没有任何意义，你需要把这个侧栏按钮换成返回按钮。

2. 侧栏
  - 当我第一次登录的时候，打开侧栏，历史聊天是空的，我必须返回主页之后，再次打开侧栏，历史聊天才会展示出来。
    - 请你修复这一问题。
  - 历史对话搜索功能：
    - 当我点击了历史会话搜索栏，弹出了键盘之后，历史会话上面的 “AI导购”、“商品”、“购物车”、“订单” 四个按钮应该能收起来，这样搜索栏下面就都是历史会话了，看得清楚一些；收起键盘之后，这些按钮要重新展示出来
    - 当我点击了历史会话搜索栏，弹出了键盘之后，点击页面的其他地方，键盘应该能自动收起来
    - 现在的历史对话搜索功能还不可用，我希望输入一段文字，你要在历史会话中**筛选出包含我刚刚输入文字的会话**
      - 也就是说，可能历史会话当中包含很多很多会话，但是包含“手机”的会话只有三个，分别是A、B、C。
      - 当我在历史对话搜索框中输入“手机”的时候，下面的历史会话应当只展示A、B、C这三个会话，用户点击之后可以进入对应会话。
      - 每当在历史对话搜索框中输入完成，不需要按“发送”，直接认为是一次搜索。
      - 搜索框清空，历史会话应当恢复成最开始展示所有会话的状态。
  - 侧栏下面的用户和设置所在的位置，背景请你用稍微深一点的灰色，和上面的历史会话区分开来。

3. 设置界面
  - 帮助、关于、退出登录等按钮最右边的 “>” 符号太大了，超过了按钮的高度。
    - 请你重新设计按钮最右边 “>” 符号的大小。
  - 头像相关
    - 点击头像之后，应当在界面的左上角加一个返回按钮。
    - 裁剪好头像之后，应该实时展示到个人资料页面的头像中。
    - 把个人资料页面右上角的 “完成” 按钮展示的字改成 “保存”。
    - 用户点击右上角的 “保存” 按钮之后，再上传到服务器，更新用户信息。
    - 现在上传头像和修改用户名的功能都无法使用，报错 “保存失败 404 not found”，是为什么？
  - 当我在设置里的 “帮助” 和 “关于” 的二级界面按返回键的时候，直接返回聊天界面了。
    - 在二级界面按返回按钮应当返回到一级界面，也就是 “设置” 界面。
    - 这两个二级界面的右上角也不要放用户设置的入口按钮。

4. 商品页面
  - 在选择商品类别的弹窗当中，默认情况下，左右两边应当都是选的 “全部”，但是默认情况下只有右边的 “全部” 按钮是被选中的样子（背景是灰色的），左边的 “全部” 按钮未被选中（背景是白色的）。
    - 默认应当两个按钮都被选中。