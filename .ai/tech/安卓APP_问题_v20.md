1. 聊天页面
  - 商品卡片渲染错误：
    - 从对话历史返回对话时，商品的渲染会出问题，包括但不限于商品消失、商品挤在一起等。
  - 商品卡片后面会有一个莫名其妙的句号。
  - 现在表格里面的 markdown 格式内容渲染有问题，尤其是加粗（**内容**）内容会直接显示出来，而没有被渲染。
  - 标题的渲染加一些颜色。
  - 关于表格，现在后端返回时会把表格用 <form></form> 标签包裹，你需要等后端完整返回表格内容后再渲染表格。
    - 接收到 <form> 标签，开始直接输出流式接收的原始内容；
    - 接收到 </form> 标签，渲染标签之间的表格。

2. 侧栏
  - 现在依旧只能展现最近的 11 条历史会话，更早的历史回话要删掉近期的才能展示出来。
    - 请你修复这一 bug。