## 后端

1. 完善用户相关接口
  - 实现用户注册、注销并提供接口
  - 实现修改个人信息（头像、昵称/用户名）并提供接口

2. 提供用户会话总结功能
  - 根据用户的会话内容，将会话总结成不超过8个字的标题
  - 用于展示到侧栏的用户对话历史中

3. 提供用户会话历史搜索功能
  - 输入聊天中出现的关键字，找到对应的会话历史

## 前端

### 针对未登录用户的核心改动

1. 未登录的用户不支持创建新会话，只在本地保留单次会话

2. 未登录用户不提供侧栏功能，点击侧栏按钮之后直接跳转到登录页面

3. “登录” 页面不应该是 “我的” 页面，而应该是单独的一个页面
  - 账号登录、注册账号等功能仍然保留


### 其他改动

1. 侧栏
  - 历史会话的问题：
    - 当前我创建了一个新会话，但我什么都没说，这个时候不要把这个空聊天同步到服务器，侧栏也不要显示这个会话
    - 只有我在会话里发送消息了，这个聊天会话才算正式开始，这个时候再把聊天同步到服务器，侧栏再开始显示这个会话
    - 也就是说，聊天记录当中不该出现空聊天
    - 每次会话结束之后，总结聊天内容，生成当前会话标题，侧栏展示的是总结好的会话标题
  - 搜索栏：
    - 搜索栏里的提示文字由 “搜索” 改成 “历史会话搜索”
    - 根据后端提供的历史会话搜索功能，在 APP 中实现历史会话搜索的功能

2. 聊天页面
  - 更改麦克风按钮的样式
    - 不要用 “mic” 三个字代替麦克风
    - 选用合适的麦克风图标
  - 当我正在输入内容的时候，点击侧栏，应当先把键盘收起来，再展示侧栏

3. 设置页面：参考图片 `/Volumes/shared/xzxg-shop/.ai/tech/assets/settings_01.jpg` 里的设计
  - 最上面显示头像、用户名、账号管理
    - 点击头像之后：
      - 放大用户头像至全屏，长按图像可保存
      - 下方设计一个 “编辑个人资料” 的按钮，点击后跳转到 “编辑个人资料” 页面
      - 参考 `/Volumes/shared/xzxg-shop/.ai/tech/assets/head_icon.jpg` 里的设计
    - 点击用户名之后也跳转到 “编辑个人资料” 页面
    - 点击 “账号管理” 按钮之后进入 “我的” 页面。
    - 上面提到的 “编辑个人资料页面” 参考 `/Volumes/shared/xzxg-shop/.ai/tech/assets/pri_info.jpg` 里的设计
      - 点击头像之后，调用图库上传头像
        - 头像只能是正方形，请你提供头像裁剪功能，将头像裁剪成正方形
      - 提供修改用户名的功能
      - 产生任何修改之后，右上角的 “完成” 按钮才能被点亮
  - 下面有 “帮助”、“关于”、“退出登录” 等按钮。
    - 设计 “帮助”、“关于” 页面，点击按钮进入对应页面。
  - 最下面放 “Version: {版本}”、“由 {大模型名称} 提供支持” 等字样。

4. 重新设计 “我的” 页面：参考 `/Volumes/shared/xzxg-shop/.ai/tech/assets/my_page.jpg` 里的设计
  - 最上方的头像、用户名、用户id**只能查看，不可点击**
  - 点击 “个人资料” 按钮之后进入前面提到的 “编辑个人资料” 页面
  - 点击 “手机号” 或 “邮箱” 后，弹窗提供手机号和邮箱的修改
  - 点击 “删除账号” 按钮之后，弹窗提醒是否确认删除，用户再次确认之后删除账号
  - 点击 “退出登录” 按钮之后，弹窗提醒是否退出登录，用户再次确认之后退出登录
  - 右上角有一个 “x” 号，点击之后和返回效果相同，也就是返回上一个页面

5. 