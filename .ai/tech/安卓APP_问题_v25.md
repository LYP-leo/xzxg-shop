1. 聊天界面
  - 当我选择图片并输入文字之后，显示 “附件上传失败：Broken pipe”
    - 请你修复这一 bug
  - 当我单独上传图片之后，无法发送不带文字，单独只有图片的消息
    - 我希望当我只上传了图片，没有输入文字的时候，也能发送消息
  - 当我按了主页面的 “+” 按钮之后，会弹出 “相册” 和 “文件”
    - 当我点击了页面的其他任何位置，现实的 “相册” 和 “文件” 部分应该收起来
    - 当我选择 “相册” 之后，我希望能跳转到相册应用选择图片，而不是文件管理器。
    - 当我选择 “文件” 之后，需要跳到文件管理器应用。现在确实会跳转到文件管理器，但默认路径是 Camera，我希望跳转到的默认目录是根目录。
  - 当我点击输入框之后，聊天窗口应该被 “推上去”，而不是像现在这样，键盘直接覆盖聊天窗口，现在这样会导致键盘覆盖聊天窗口最下面的内容，用户需要手动向下滑动才能看见。
    - 简单来说，聊天框的最下端需要和键盘的最上端接在一起，保证用户随时能看到最新聊天内容。
  - 当我键盘显示出来的时候，点击键盘外的部位，应当能够自动收起键盘。
  - 当我选择了图片之后，切换到另一个聊天，图片依然显示
    - 当我切换到另一个聊天的时候，请你不要显示我刚刚选择的图片
    - 当我切换回原来的聊天的时候，图片依然能显示出来
    - 也就是说，对于我选择的图片和文件等内容，每个对话都是独立的。

  