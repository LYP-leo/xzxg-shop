1. 会话的实现
+ 三个表：chat_session、user_message、assistant_message
  + 一个对话组/元会话是一个 chat_session，一次元会话中可以包含多次单轮对话
    + ![alt text](b2de3f1fb8d02c1473840966d4a441eb.jpg)
    + 上图：一次元会话包含了两次对话（鹦鹉推荐和安静陪伴）
  + 一次对话是一个 user_message
  + 一次对话中的一次 llm 调用是一个 assistant_message
    + 
2. assistant_message 不需要状态，只需要在结束之后记录做日志就好了
3. 整理链路中所有的 llm 调用，输入格式输出格式和提示词
4. 不需要回答策略，统一结构：主 agent 输出的文本 + 一个追问 llm 调用生成追问，具体的策略放到主 agent 里，根据意图动态拼接的 prompt 里
5. AgentResponse，agent 一个字一个字往外蹦输出，前端怎么实时流式解析格式，感觉 xml 比 json 靠谱
6. 切片为什么不是递归按照标点符号切片之类的
7. 设计下具体工具


补完系统其他部分的细节，编写技术方案_v2.MD，要求根据技术方案可以完整落地系统，没有模糊点