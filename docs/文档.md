# 关键问题解决方案

本文档总结小猪小狗电商导购项目中几个关键技术问题的处理方式。整体思路不是把大模型直接接到商品库上，而是围绕真实购物链路建立一套可控的 Agent 系统：前置风控、意图路由、工具调用、真实商品召回、流式渲染、记忆补充、动态 prompt、质量测评和业务闭环共同约束最终体验。

## 系统架构

项目整体采用“多端入口 + Go 后端服务 + Agent Runtime + 数据与配置中间件”的架构。前端侧包含 React Web 客户端、Capacitor 移动端工程和原生 Android 客户端，分别覆盖用户、商家、管理员和移动端导购场景；后端侧由 Go API 统一承接 HTTP、SSE、鉴权、业务接口、Agent 对话、图片搜索、评测接口和后台管理能力。

从调用链路看，用户在前端发起导购请求后，请求进入 `/api/v1/agent/sessions/{session_id}/messages:stream`，HTTP 层完成鉴权、限流、日志、请求体限制和上下文注入，然后创建用户消息与 Agent run。Agent Runtime 再依次执行风控检查、图片能力降级判断、短期记忆检索和意图路由：导购类 `guide` 请求继续细分 P1-P6 并进入 ReAct 工具循环；非导购类 `non_guide` 请求中，如果是加购、结算等确定性动作，会直接进入固定动作执行器，避免模型只口头承诺但没有真正写购物车或订单。整个过程中，文本和结构化 block 通过 SSE 返回前端，trace、run 状态和最终结果写入数据库，方便管理员回看。

数据层以 MySQL 为业务主存储，保存账号、会话、消息、run、trace、商品、SKU、商家、购物车、订单、优惠、评价和知识库文档等数据。Nacos 负责动态配置和 prompt 配置，支持在不重启服务的情况下调整模型、prompt、工具策略、检索参数和风控规则。Milvus 负责商品、知识库和商品图片的向量检索，etcd 和 MinIO 作为向量服务依赖与对象存储组件；图片文件、上传内容和商品图片检索也可以通过 MinIO 与图片 embedding 链路衔接。Redis 当前作为预留中间件，后续可用于缓存、限流或异步任务状态。

系统可以简化为七层。业务入口在最上方，核心链路放在中间，工具、数据、模型和基础设施作为支撑层承接执行。

```text
┌──────────────────────────────────────────────────────────────┐
│ 业务层                                                       │
│ 用户导购      商品交易      商家运营      平台管理            │
└──────────────────────────────────────────────────────────────┘
                                │
                                v
┌──────────────────────────────────────────────────────────────┐
│ 接入层                                                       │
│ Web前端       移动前端      原生安卓      管理后台            │
└──────────────────────────────────────────────────────────────┘
                                │
                                v
┌──────────────────────────────────────────────────────────────┐
│ 应用层                                                       │
│ 统一接口      流式接口      鉴权限流      日志追踪            │
└──────────────────────────────────────────────────────────────┘
                                │
                                v
┌──────────────────────────────────────────────────────────────┐
│ 核心链路                                                     │
│ 用户提问 -> 风控记忆 -> 意图路由 -> 工具执行 -> 流式返回 -> 业务落库 │
└──────────────────────────────────────────────────────────────┘
              │                         │                         │
              v                         v                         v
┌──────────────────────┐   ┌──────────────────────┐   ┌──────────────────────┐
│ 编排层               │   │ 工具层               │   │ 数据层               │
│ 风控检查  记忆检索   │   │ 商品检索  知识检索   │   │ 业务数据  会话记录   │
│ 意图路由  导购分支   │   │ 购物工具  订单工具   │   │ 运行追踪  提示版本   │
│ 动作分支  追踪记录   │   │ 挂品校验  结果回填   │   │ 配置中心  向量检索   │
└──────────────────────┘   └──────────────────────┘   └──────────────────────┘
              │                         │                         │
              └──────────────┬──────────┴──────────┬──────────────┘
                             v                     v
┌──────────────────────────────────┐   ┌──────────────────────────────────┐
│ 模型层                           │   │ 基础层                           │
│ 小型模型  大型模型  文本向量      │   │ MySQL     Nacos     Milvus       │
│ 图片向量  相关判断  追问生成      │   │ MinIO     Redis     本地兜底     │
└──────────────────────────────────┘   └──────────────────────────────────┘
```

另补一个 Excalidraw 版本，保留上面的文本分层图不变：[xzxg-shop-architecture.excalidraw](/Users/keii/xzxg-shop/docs/xzxg-shop-architecture.excalidraw)，PNG 预览见 [xzxg-shop-architecture.png](/Users/keii/xzxg-shop/docs/xzxg-shop-architecture.png)。

这个架构的关键决策是让 Agent 不是独立的聊天模块，而是嵌入真实电商业务系统。Runtime 不直接操作数据库细节，而是通过 Store 接口调用商品、购物车、订单、知识库和 trace 能力；配置不写死在代码里，而是由 Nacos 和后台配置页面管理；模型输出不能直接决定业务动作，而要经过工具策略、商品白名单、风控和事务接口约束。这样做牺牲了一部分实现速度，但换来了可控性、可观测性和业务闭环。

亮点在于系统既支持本地开发兜底，也能向生产形态扩展：Nacos 不可用时可退回内存默认配置，模型 key 未配置时有本地兜底回答，Milvus 不可用时商品和知识仍可走 MySQL 关键词检索；一旦中间件配置完整，又可以启用动态 prompt、向量召回、图片搜索和后台 trace 分析。后续可以继续改进的是把架构中的异步任务拆得更清晰，例如向量索引构建、图片 embedding、评测任务和订单超时关闭可以逐步独立为后台 worker，降低 API 进程的启动和运行压力。

## 流式输出与渲染协议
流式输出已经成为agent的标配，它解决了等待延迟与交互断层问题，大幅提升用户体验、适配多场景交互的核心技术。
导购 Agent 的回答需要像聊天一样实时输出，

又要能稳定展示商品卡、购物车、订单、警告等结构化内容。
如果只让模型输出一整段 Markdown，前端很难可靠识别哪些内容是自然语言，哪些内容应该渲染成卡片；如果等模型完整生成后再统一解析，又会牺牲流式体验。

本项目采用 SSE 事件作为前后端渲染协议。后端在 Agent 运行开始时发送 `message_start`，中间用 `status` 告知“理解需求、调用工具、生成回答”等阶段；自然语言正文通过 `text_delta` 增量输出，商品卡、购物车摘要、订单摘要、风控提示等结构化结果通过 `block_delta` 输出，最后用 `followups` 和 `message_end` 收束。前端只需要按事件类型更新当前 turn，不需要猜测模型输出里的业务结构。

核心决策是把“模型最终回答”和“前端结构化渲染”拆开。模型可以在 `<final>...</final>` 中输出面向用户的自然语言，并用 `<item>product_id</item>` 表示商品卡插入位置；后端会检查这个 `product_id` 是否来自当前轮工具返回的白名单，只有通过校验的商品才会透出给前端。这样既保留了 token 级流式输出，又避免模型编造商品卡、错误挂品或把内部工具观察暴露给用户。

亮点在于协议足够简单但边界清晰：文本、状态、业务 block、追问问题各自独立；取消生成时可以通过 run 状态及时中断；管理员还可以通过 trace 看到每一步模型输出、工具参数和 observation。后续可以继续完善的是更细粒度的内容分段，例如把表格、引用资料、对比维度也拆成稳定 block，减少前端对 Markdown 的依赖。

## 多模态交互

电商场景天然有图片需求，用户可能希望“拍照找同款”“按图搜商品”或上传商品截图。这里的问题是，多模态能力一旦接入不完整，很容易出现模型假装识别图片、给出不可靠结论的情况；但如果完全不支持图片，又会缺少电商导购的重要入口。

项目里把图片能力拆成两层：独立图片搜索接口和聊天中的图片降级处理。`/api/v1/search/image` 支持通过 `file_id`、`object_key` 或 `image_url` 生成图片向量，再到 Milvus 的商品图片集合中检索相似商品；图片向量默认可使用 DashScope `qwen3-vl-embedding`，同时保留本地颜色直方图作为兜底。聊天主链路里如果用户提出拍照找货，但当前没有配置 VLM 图片识别服务，Agent 会明确降级提示用户改用文字描述，而不是伪造识别结果。

这个决策的重点是把“以图搜相似商品”和“理解图片内容”区分开。前者可以依赖图片 embedding 和向量库做召回，后者需要 VLM 对图片语义负责；在 VLM 未配置时，系统宁可提示能力边界，也不把图片当作已经识别过的事实注入大模型。

亮点是多模态链路已经预留了真实工程入口：文件存储走 MinIO，图片检索走向量库，接口返回耗时拆分为下载、embedding、向量搜索等阶段，方便后续评测和排查。后续可改进的是把图片搜索结果接入 Agent 工具体系，让用户在聊天里上传图片后，Agent 能基于相似商品继续做对比、加购和追问。

## Prompt 动态拼接与成本管控

真实购物场景中用户意图差异很大：有的只是问候，有的是商品推荐，有的是对比决策，有的是售后、优惠、订单或购物车动作。如果每一轮都把所有规则、所有工具、所有业务知识一次性塞进大 prompt，会增加 token 成本，也会让模型更容易调用不相关工具。

项目采用“先路由、再拼接”的策略。前置小模型先做一级路由：`guide`、`non_guide`、`fast_product`；导购类问题再细分到 P1-P6，例如单品深问、对比决策、穿搭、品类选购、场景方案和开放探索。进入 ReAct 执行阶段后，系统 prompt 再按当前 intent 动态拼接基础回答规则、工具调用协议、可用工具、可用 skill、当前子意图要求和最终输出规范。

模型选择上也做了分层：意图识别、记忆判断、追问生成等相对轻量的任务使用 small model；复杂导购回答和 ReAct 主循环根据意图使用 answer model。动态配置放在 Nacos 中，`ai.base_url`、`ai.api_key`、大小模型、thinking 开关、prompt 模板、工具策略、检索参数和风控规则都可以通过后台调整；后端有短缓存，避免每个流式 token 都访问配置中心。

亮点是 prompt 不再是写死的一整段系统提示，而是运行时根据“用户意图 + 工具策略 + 配置中心”组合出来。这样既方便迭代 prompt，也能控制不同场景的成本和工具暴露面。后续可以继续做的是把每个 intent 的 token 消耗、工具调用次数、成功率和用户满意度关联起来，形成更精细的模型路由和 prompt 版本回滚机制。

## 真实商品召回

导购推荐最核心的问题是不能只生成“看起来合理”的建议，而必须基于真实商品库。否则模型可能编造商品、价格、库存或卖点，用户也无法继续加购、下单，整个链路会断在推荐结果上。

项目中商品召回走真实数据源。商品、SKU、商家、库存、价格、卖点、风险提示等信息存储在 MySQL 中；商品和知识库可以接入 Milvus 向量检索；图片商品则使用商品图片向量集合。`search_products` 工具先做关键词召回，并在向量库可用时合并向量召回结果，再按商品相关性、结构化约束、品牌/型号/类目等信号重排。知识库检索同样支持关键词和向量召回，并经过统一 rerank 和摘要截断。

为了避免“召回到了但不该挂给用户”的问题，项目还加入了相关性准入和商品白名单机制。`search_products` 会记录候选商品、通过商品、被剔除商品和相关性原因；只有相关性通过的商品 ID 才能进入当前轮允许挂品列表。后续加购也不能凭模型猜测 product_id，必须来自当前轮明确允许的商品或相关记忆中的商品。

亮点是召回链路不只看“能不能搜到”，还关注“能不能安全地给模型用、能不能安全地给前端挂卡、能不能继续加购下单”。后续可以继续提升的是引入更成熟的混合检索权重学习、商品属性结构化过滤、库存与优惠实时特征，以及基于 bad case 的召回评测集自动扩充。

## 记忆与改写

电商对话的上下文很特殊。我们在项目初期考虑过两种产品形态：一种类似豆包，每次继承上次对话；一种类似 GPT，每次打开时新开对话。电商场景里，用户每一次购物需求可能完全不同，比如今天买洗面奶，下一次可能买鼠标、咖啡或跑鞋，过度继承历史反而会干扰当前决策。

因此当前记忆模块采用短期、按需注入的方式。每轮对话开始前，系统从当前会话中检索最近若干轮记录，默认窗口是 24 小时内最近 5 轮；小模型判断这些记录是否和当前问题直接相关，只有当用户出现“刚才那个”“第二个”“继续看”“加入购物车”等依赖上下文的表达时，才把相关摘要、商品 ID 和商品顺序注入当前 query。没有模型配置时，也有基于关键词的启发式兜底。

这个决策避免了两类问题：一是无关历史污染当前购物需求，二是用户指代历史商品时系统无法解析。记忆注入后，Agent 仍然必须围绕当前问题回答，历史只作为指代消解和偏好补充；引用商品 ID 也会被限制在候选历史中实际出现过的商品，防止凭空生成。

一个典型 bad case 是：用户上一轮看的是跑鞋，这一轮问“第二个加购物车”；如果系统只按最近商品或模型猜测，很容易加错商品。当前做法会在记忆 prompt 中保留“本轮商品顺序”，并要求当“第 N 个 + 品类词”出现时优先选择同品类、同主题的推荐轮次。后续可以继续完善跨会话长期偏好，例如稳定尺码、预算区间、过敏成分、品牌偏好，但需要明确有效期和用户可见的记忆管理能力。

## 风控与安全

生产场景中，导购 Agent 不能只追求回答能力，还必须考虑平台风控、账号状态、商家和商品安全。风险一旦进入大模型上下文，既可能造成违规输出，也可能污染后续推荐结果。

当前采用三个维度做风控。第一是用户 query 维度：用户某次输入中包含违规内容时命中风控，规则支持关键词和正则模式，并可通过配置中心调整。第二是用户维度：账号被标记为风险状态时，Agent 直接停止服务并给出提示。第三是商家/商品维度：风险商家或风险商品不会在正常召回中透出给后续大模型，避免模型推荐不可售或不应展示的商品。

在用户 query 风控上，我们在“小模型判断”和“规则匹配”之间做了取舍。小模型的优点是灵活，可以理解更多变体表达；但考虑到提示词绕过、模型投毒、对抗样本、额外 token 成本和额外 RT，当前选择规则匹配作为第一层硬拦截。规则虽然表达能力有限，但行为稳定、成本低、可解释，也更适合作为进入 Agent 前的基础防线。

亮点是风控发生在主链路前置阶段，并且结果会通过 `warning` block 和 trace 记录出来，前端与后台都能看到原因。后续可以考虑增加分层风控：高置信规则直接拦截，低置信风险进入审核或小模型辅助判断；同时把商家资质、商品状态、售后异常率等更多业务风险接入召回过滤。

## 测评

Agent 项目如果只靠主观试聊，很难判断每次 prompt 或检索参数调整到底是进步还是退步。因此项目建立了多组面向不同链路的测评，把线上同一套接口和运行逻辑纳入评估，减少“测评逻辑和线上逻辑不一致”的问题。

目前已有的测评包括：意图分类测评，关注 query 是否被正确分到一级路由和具体 intent；RAG 召回测评，关注关键词是否召回预期商品或知识；Agent 端到端测评，覆盖导购、非导购、购物车动作、库存不足、图片降级、售后、优惠和订单等常见入口；记忆检索测评，验证指代消解和历史商品引用；图片搜索测评，验证图片向量检索的命中情况。脚本统一放在 `quality/evals`，数据放在 `quality/data/eval`，报告输出到 `quality/reports`。

这个设计的亮点是把复杂 Agent 拆成可单独观察的环节。意图错了就看路由，召回错了就看检索和 rerank，回答错了再看 ReAct trace、工具 observation 和最终输出协议。这样排查问题时不需要只盯最终答案，而能定位到底是分类、召回、工具、记忆、prompt 还是渲染协议的问题。

后续可以继续改进的是自动打分能力。当前端到端测评更多依赖人工检查，未来可以为每类场景定义结构化断言，例如是否调用了正确工具、是否出现了预期商品、是否没有编造价格、是否正确拒绝无能力场景，并引入回归阈值用于发布前阻断。

## 开发范式

这个项目的核心不是单一页面或单一模型调用，而是一套完整的电商 Agent 工程。因此开发范式上强调“业务链路真实、模块边界清晰、可配置、可观测、可回归”。

后端以 Store 接口隔离数据访问，HTTP handler 和 Agent runtime 不直接依赖 MySQL 细节；Agent runtime 内部按风控、记忆、planner、ReAct、tool、skill、followups、trace 分层；前端按用户、商家、管理员三个角色组织页面；质量目录独立维护评测数据和脚本；Docker Compose 提供 MySQL、Nacos、Milvus、MinIO、Redis 等本地中间件。这样做的好处是每个能力都能单独替换或扩展，而不是堆在一次模型调用里。

另一个重要范式是可观测。每个 run 都有 trace，记录 planner、memory、LLM、tool、retrieval、answer、followup 等事件；管理员页面可以查看 prompt/config、Agent trace、向量状态和质量报告。这让 prompt 调整、召回调参和 bad case 排查有了依据。

后续可以继续提升的是工程发布流程：把 prompt 版本、配置变更、评测报告和灰度发布绑定起来；同时补充更完整的 CI 测试矩阵，覆盖 Go 单元测试、前端构建、关键 API 冒烟和评测脚本抽样运行。

## 其他

除了导购对话本身，项目还模拟了真实电商链路：用户登录鉴权、商品列表和详情、购物车加购与改数量、结算下单、虚拟支付、订单状态、优惠券、促销、评价、商家商品管理、商家订单管理、管理员账号/商品/订单/文档/配置管理等。这些能力让 Agent 的推荐不只是“说”，而是能继续落到加购、下单、查看订单等真实动作。

当前比较有价值的设计是把 Agent 能力嵌入电商业务闭环。模型推荐出的商品必须来自商品库；加购必须经过工具和商品 ID 校验；下单需要购物车事务、库存预留、订单和支付记录；管理员可以配置 prompt 和风控规则，也能查看 Agent trace 和质量报告。

后续可以继续完善的方向包括：更完整的商品属性体系、真实支付和物流对接、商家审核流、用户画像和长期偏好、A/B 实验、客服售后工单、多模态聊天工具化，以及更精细的权限与审计体系。
